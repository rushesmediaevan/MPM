/**
 * Motion contract for MPM site — aligns with global.css reveal easing.
 * React Bits–style effects use these instead of ad-hoc values.
 */
export const EASE_OUT = [0.16, 1, 0.3, 1]

export const DURATION = {
  fast: 0.32,
  headline: 0.48,
  rule: 0.85,
}

/** Delay between staggered words (hero headline). */
export const STAGGER_WORD = 0.052
