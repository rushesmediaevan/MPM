MPM landscaping static ad backgrounds for /ads/landscaping

Current files (repo — replace with your Recraft exports when ready):

  mpm-land-01-hero.jpg      — real MPM porch baseline (bridge batch asset)
  mpm-land-02-install.jpg  — real before-yard (bridge batch asset)
  mpm-land-03-lawn.jpg      — real bench walk wide (bridge batch asset)

Logo: ../mpm-logo-white.png (copied from clients/mpm/ads/static/.../assets)

Overlay copy + CTA live in src/pages/LandscapingAdsPage.jsx — not baked into JPGs.
